type AcceptInvitePayload {
    response: String!
}

extend type Mutation {
    acceptInvite(
        inviteId: ID!,
        inviteBoardId: String!,
        userId: ID!,
        boardsIds: [ID!]!
    ): AcceptInvitePayload
}
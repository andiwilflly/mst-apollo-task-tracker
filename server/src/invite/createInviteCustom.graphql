type CreateInvitePayload {
    response: String!
}

extend type Mutation {
    createInviteCustom(
        authorId: String!,
        boardId: String!,
        emailInviteReceiver: String!,
    ): CreateInvitePayload
}
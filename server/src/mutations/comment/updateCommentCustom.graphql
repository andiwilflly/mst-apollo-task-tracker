type UpdateCommentCustomPayload {
    response: String!
}

extend type Mutation {
    updateCommentCustom(
        id: ID!,
        authorId: ID,
        taskId: ID,
        text: String
    ): UpdateCommentCustomPayload
}
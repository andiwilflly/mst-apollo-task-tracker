type UpdateCommentCustomPayload {
    response: String!
}

extend type Mutation {
    updateCommentCustom(
        id: ID!,
        text: String,
        authorId: ID,
        taskId: ID
    ): UpdateCommentCustomPayload
}
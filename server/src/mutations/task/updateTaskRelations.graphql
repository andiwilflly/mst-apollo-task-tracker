type UpdateTaskRelationsPayload {
    response: String!
}

extend type Mutation {
    updateTaskRelations(
        id: ID,
        boardId: ID,
        listId: ID,
        authorId: ID
    ): UpdateTaskRelationsPayload
}
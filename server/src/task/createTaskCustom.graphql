type CreateTaskCustomPayload {
    task: String!
    user: String!
    board: String!
    list: String!
}

extend type Mutation {
    createTaskCustom(title: String!, description: String!, boardId: ID!, authorId: ID!, listId: ID!): CreateTaskCustomPayload
}
type CreateTaskCustomPayload {
    task: String!
    user: String!
    board: String!
    list: String!
    labels: String!
}

extend type Mutation {
    createTaskCustom(title: String!, description: String!, boardId: ID!, authorId: ID!, listId: ID!, labelsIds: [ID!]!): CreateTaskCustomPayload
}
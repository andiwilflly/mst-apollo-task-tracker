type DeleteTaskCustomPayload {
  board: String!
  user: String!
  list: String!
}

extend type Mutation {
  deleteTaskCustom(taskId: ID!, boardId: ID!, userId: ID!, listId: ID!): DeleteTaskCustomPayload
}
type UpdateTaskRelationsPayload {
    response: String!
}

extend type Query {
    updateTaskRelations(
        id: ID,
        boardId: ID,
        listId: ID,
        authorId: ID
    ): UpdateTaskRelationsPayload
}